def findsmallestElement(numbers):
    result=500
    n=len(num)
    for index in range(n):
        if num[index]<result:
           result=num[index]
    return result
num=[12,23,45,56,67,89,100,245]
result=findsmallestElement(num)
print("smallest  element is",result)
